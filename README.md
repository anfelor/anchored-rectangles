# Solver for the Lower-Left Anchored Rectangle Problem

You can open a GUI with `./gradlew run`.

The lab report is at `docs/report.pdf` and the sources are at `app/src/main/java/org/anchoredrectangles`.
